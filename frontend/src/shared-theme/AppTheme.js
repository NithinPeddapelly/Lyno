import { createTheme } from "@mui/material/styles";

const AppTheme = createTheme({
  palette: {
    primary: {
      main: "#574bff",
    },
    secondary: {
      main: "#000000",
    },
  },
});

export default AppTheme;
