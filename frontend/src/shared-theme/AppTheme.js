import { createTheme } from "@mui/material/styles";

const AppTheme = createTheme({
  palette: {
    mode: "dark",
    primary: {
      main: "#041d02",
    },
    secondary: {
      main: "#4ce031",
    },
  },
});

export default AppTheme;
